"""Neural codecs and density methods (Part 2).

Encoder (sequence → embedding), decoder (embedding → sequence), Pgen-from-embedding
regressor, and continuous-density background subtraction. Requires the ``[ml]`` extra
(PyTorch). Empty in v3.0.
"""
